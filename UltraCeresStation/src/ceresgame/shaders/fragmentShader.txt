#version 420 core

in vec3 colour;
in vec2 pass_textureCoordinates;

out vec4 out_Color;

uniform sampler2D objectTexture;

void main(){
    
    //out_Color = texture(objectTexture, pass_textureCoordinates);
    out_Color = vec4(0, 0, 1, 1)

}
