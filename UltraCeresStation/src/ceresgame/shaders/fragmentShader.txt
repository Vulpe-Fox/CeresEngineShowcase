#version 420 core

in vec3 colour;
in vec2 pass_textureCoordinates;

out vec4 out_Color;

uniform sampler2D texture;

void main(){

    outColor = vec4(colour, 1.0);
    //out_Color = texture(texture, pass_textureCoordinates);

}
